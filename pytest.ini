[pytest]
# Discover BDD and unit tests only below the project test tree.
testpaths = tests
pythonpath = .
# Enforce markers, activate both reports, and use the custom compact terminal view.
addopts =
    -v
    --strict-markers
    --import-mode=importlib
    -p no:cacheprovider
    -p no:sugar
    --html=results/reports/report.html
    --self-contained-html
    --cucumberjson=results/reports/cucumber-report.json
# Describe every supported selector used by Docker, local runs, and CI.
markers =
    smoke: critical-path validation
    regression: complete functional regression suite
    positive: valid business behavior
    negative: validation or error behavior
    ui: web user-interface automation
    api: REST API automation
    destructive: modifies persistent data or account balances
